package com.zhentao.review.google.todo;

/**
 * 无限大矩阵，一开始在(0,0)格子里，每次移动可以跳到2*3矩阵的另外一头的格子，例如(0,0)可以跳到(1, 2), (2, 1), (2, -1),
 * (1, -2), (-1, -2), (-2, -1), (-2, 1), (-1, 2)八个格子。 给一个target (x,
 * y)，问最少跳到target最少需要几步。写了个bfs。
 * 
 * @author zhentao
 *
 */
public class KnightJumper {

}
